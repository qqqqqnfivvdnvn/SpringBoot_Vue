在主数据的项目模块加入机构模糊匹配 （java文件、vue文件命名风格和主数据项目保持一致）
#前端项目需求
#首先是主页，文件名在maindataproject目录创建一个模糊匹配（使用英文）的文件夹，里面包含两个模块和区县经纬度的一致
1、批次列表 与经纬度的模块设计风格一致
2、文件上传 与经纬度的模块设计风格一致
3、汇总数据 与经纬度的模块设计风格一致

#后端的批次表 数据源采用master_sqlserver源
haosen_project..md_fuzzy_match_batch(batch_id(uuid),status(0处理中 1已处理 2 处理失败)	original_filename(文件名称)	message	create_time	update_time)

#数据汇总表
batch_id	id	original_name	original_province	original_address,keyid,name ,remark ,create_time，update_time

#上传的excel 文件字段（ 使用ReaderExcel.java读取 ）
id、省份、名称、地址
情况1 省份不为空时
将 名称 拆分成下面格式例如 原始名称 杨永发西医诊所，拆成：%杨%永%发%西%医%诊%所，去匹配
sql = 'SELECT keyid,name from HospitalInfo..hospital(nolock) WHERE status=1 and ( name like  +str(
            item['orginal_name']) and province='" + item['province'] + "'"

如果返回的结果不是一条数据，执行下面的
sql = 'SELECT   keyid,name from HospitalInfo..hospital(nolock) WHERE status=1 and ( name like  ' + "'%" + str(
            item['orginal_name']) + "%'" + "  or namehistory like " + "'%" + str(
            item['orginal_name']) + "%') and province='" + item['province'] + "'"

如果返回的结果不是一条数据，执行下面的
根据 省市区简称.xlsx（这个可以写库也可以json，怎样方便怎样来）（省，province_short，市，city_short，区县，area_short）将原始名称中省市区和简称替换掉

再执行下面步骤
sql = 'SELECT keyid,name from HospitalInfo..hospital(nolock) WHERE status=1 and ( name like  +str(
            item['orginal_name']) and province='" + item['province'] + "'"

如果返回的结果不是一条数据，执行下面的
sql = 'SELECT   keyid,name from HospitalInfo..hospital(nolock) WHERE status=1 and ( name like  ' + "'%" + str(
            item['orginal_name']) + "%'" + "  or namehistory like " + "'%" + str(
            item['orginal_name']) + "%') and province='" + item['province'] + "'"

如果返回的结果不是一条数据，重复执行上面的步骤， sql语句不同
      sql = 'SELECT top 3 key_id,name from HospitalInfo..company(nolock) WHERE status=1 and ( name like  ' + "'%" + str(
            item['orginal_name']) + "%'" + "  or history_name like " + "'%" + str(
            item['orginal_name']) + "%') and province='" + item['province'] + "'"


情况2 省份为空的情况下将上面的sql语句中省份去除即可 



