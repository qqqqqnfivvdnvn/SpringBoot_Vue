<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.UserProjectRelationMapper">

    <insert id="insert" parameterType="com.example.demo.entity.UserProjectRelation">
        INSERT INTO [haosen_project].[dbo].[user_project_relation] ([user_id], [project_id], [permission_type], [create_time], [creator_id])
        VALUES (#{userId}, #{projectId}, #{permissionType}, GETDATE(), #{creatorId})
    </insert>

    <delete id="delete">
        DELETE FROM [haosen_project].[dbo].[user_project_relation] WHERE [user_id] = #{userId} AND [project_id] = #{projectId}
    </delete>

    <select id="findByUserId" resultType="com.example.demo.entity.UserProjectRelation">
        SELECT * FROM [haosen_project].[dbo].[user_project_relation] WHERE [user_id] = #{userId} ORDER BY [create_time] DESC
    </select>

    <select id="findByProjectId" resultType="java.util.Map">
        SELECT r.user_id, r.project_id, r.permission_type, r.create_time, r.creator_id, u.username
        FROM [haosen_project].[dbo].[user_project_relation] r
        LEFT JOIN [haosen_project].[dbo].[Users] u ON r.user_id = u.id
        WHERE r.project_id = #{projectId}
        ORDER BY r.create_time DESC
    </select>

    <select id="findByUserAndProject" resultType="com.example.demo.entity.UserProjectRelation">
        SELECT * FROM [haosen_project].[dbo].[user_project_relation] WHERE [user_id] = #{userId} AND [project_id] = #{projectId}
    </select>

    <update id="updatePermission">
        UPDATE [haosen_project].[dbo].[user_project_relation] SET [permission_type] = #{permissionType}
        WHERE [user_id] = #{userId} AND [project_id] = #{projectId}
    </update>

</mapper>
