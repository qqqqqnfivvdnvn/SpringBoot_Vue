创建主数据的项目 (不要改我其他的项目代码，如HaoSen和HengRui开头的Java文件和vue文件)
#前端项目需求 
#首先是主页，文件名在maindataproject目录下创建MainDataHome.vue,其中页面模版和HengRuiHome.vue的保持一致 ,样式风格保持一致，内容需要修改为主数据的页面内容，包括页面的标题，文字描述等。
#保留恒瑞中的批次管理（批次管理模块、文件上传模块）和数据汇总模块（汇总数据模块），其他不需要，这两个模块与恒瑞的样式风格保持一致。

#后端项目需求 
#上传文件模块，excel上传的文件模板（id,省份，名称，地址）
#和恒瑞一样采用异步的处理方式
#首先先验证excel表结构（ExcelHeaderValidator.java）,再解析excel（使用ReaderExcel.java） 
#要先使用uuid生成一个批次号，插入到数据表中，表名（haosen_project..md_location_batch 表字段  batch_id(批次)	create_time（插入日期）	update_time（更新）	status（0：处理中 1：已处理 2 处理失败 ）	original_filename（上传的文件名称）	message（处理成功保留处理完成，失败保留报错信息） ）
#解析完excel文件，需要调用一个文件中的api(gaode_api文件中的方法，里面是python写的，需要改成java代码，并改成一个工具放在utils包下），
#这个接口传入省份和地址的参数，返回json
#接口返回的json是{'infocode': '10000', 'info': 'OK', 'location': '105.857014,32.427191', 'province': '四川省', 'city': '广元市', 'area': '利州区', 'areaid': '510802', 'address': '广元市利州区苴国路490号'}
#需要将数据插入到 haosen_project..md_location表中
其中表字段
batch_id(批次)	id（id）	original_name(文件中名称)	original_province(文件中省份)		original_address(文件中地址)	area_name（接口中area）	area_id（接口中areaid）	lng_lat（接口中location）	

前端的文件导出需要根据批次号查询出数据，然后导出excel文件（使用EasyExcel）

#写完成后，检查代码有没有报错








